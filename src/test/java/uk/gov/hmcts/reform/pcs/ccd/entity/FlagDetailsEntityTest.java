/*
package uk.gov.hmcts.reform.pcs.ccd.entity;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.mock;

class FlagDetailsEntityTest {

    private FlagDetailsEntity underTest;

    @BeforeEach
    void setUp() {
        underTest = new FlagDetailsEntity();
    }

    @Test
    void shouldUpdateFlagDetailsEntity() {
        // Given
        FlagsEntity updatedFlagsEntity = mock(FlagsEntity.class);

        // When
        underTest.setCaseFlag(updatedFlagsEntity);

        // Then
        assertThat(underTest.getCaseFlag()).isSameAs(updatedFlagsEntity);
    }
}
*/
