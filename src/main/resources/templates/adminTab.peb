{% macro showPartyRow(party) -%}
  | {{ party.forename }} {{ party.surname }} |{{ party.idamId }} | {% if party.pcqId is not null %} {{ party.pcqId }} {% else -%} Not Set {%- endif %}|
{%- endmacro %}

## Parties

{% if partyList | length > 0 %}

| Name | Idam ID | PCQ ID |
|------|---------|--------|
{% for party in partyList %}
  {{- showPartyRow(party) }}
{% endfor %}

{% else %}
### No parties have been added to this case
{% endif %}

