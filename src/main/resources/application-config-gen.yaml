spring:
  datasource:
    url: ${testcontainer.datasource.url}
    driver-class-name: ${testcontainer.datasource.driver}
    username: ${testcontainer.datasource.username}
    password: ${testcontainer.datasource.password}
  jpa:
    database-platform: ${testcontainer.jpa.platform}
    hibernate:
      ddl-auto: none
    show-sql: true
