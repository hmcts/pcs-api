// Threshold for the [slowValidation] log. A validation on a rendered page is milliseconds; a
// second or more means something is waiting rather than reading.
export const SLOW_VALIDATION_MS = 3000;
