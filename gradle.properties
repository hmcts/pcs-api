org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.caching=true
# Gates cftlibTest (and its docker compose) in CI's `check` - flip to true before merging to master
cftlibTestEnabled=false
