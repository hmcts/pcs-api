config.stopBubbling = true

# Copy @Qualifier onto the constructor parameter Lombok generates for a @RequiredArgsConstructor /
# @AllArgsConstructor field, so qualified beans (e.g. the IdamTokenProvider variants) resolve correctly.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
